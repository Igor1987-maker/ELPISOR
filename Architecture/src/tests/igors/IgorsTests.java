package tests.igors;

import java.math.BigDecimal;
import java.util.List;

import org.junit.After;
import org.junit.Before;
import org.junit.Ignore;
import org.junit.Test;
import org.junit.experimental.categories.Category;
import org.openqa.selenium.WebElement;

import com.google.sitebricks.client.Web;

import pageObjects.edo.NewUXLoginPage;
import pageObjects.edo.NewUxHomePage;
//import testCategories.categoryIgorS;
import testCategories.unstableTests;
import testCategories.edoNewUX.HomePage;
import tests.edo.newux.BasicNewUxTest;
import tests.misc.EdusoftWebTest;
import Enums.ByTypes;
import Interfaces.Sanity;
import Interfaces.TestCaseParams;

public class IgorsTests extends EdusoftWebTest {
	
			
		@Test
		public void Lesson1() throws Exception {

		
						
			
			/*report.startStep("Open URL");
			webDriver.openUrl("https://www.wikipedia.org/");
			
			report.startStep("Find element");
			WebElement searchField = webDriver.waitForElement("search", ByTypes.name);
			
			report.startStep("Insert text");
			searchField.sendKeys("selenium forever");
			
			report.startStep("Get text");
			WebElement englishElement = webDriver.waitForElement("js-link-box-en", ByTypes.id);
			String englishText = englishElement.getText();
			
			report.startStep("Press button");
			englishElement.click();
						
			report.startStep("Wait 3 seconds");
			sleep(3);
			
			report.startStep("Verify search results header text");
			testResultService.assertEquals("English", englishText);*/
			
		}
		
		
		
}
